#ifndef JLIBC_BITUTIL_H_
#define JLIBC_BITUTIL_H_

#include <stdio.h>
#include <stdint.h>

void bitutil_print_8bits(uint8_t x);
void bitutil_print_32bits(uint32_t x);
void bitutil_print_64bits(uint64_t x);

#endif
